
const Events = ({ day }) => {
    console.log("day", day)
    return (
      <div>
        {/* {day.morning_activities.map((itm, indx) =>{
            return(
                <ul key={indx}>
                    <li> {itm} </li>
                </ul>
            )
        })}
        {day.afternoon_activities.map((itm, indx) =>{
            return(
                <ul key={indx}>
                    <li> {itm} </li>
                </ul>
            )
        })}
        {day.evening_activities.map((itm, indx) =>{
            return(
                <ul key={indx}>
                    <li> {itm} </li>
                </ul>
            )
        })} */}
      </div>
    )
  }

  export default Events
